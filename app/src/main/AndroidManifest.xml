<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.proyecto">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme_NoActionBar"
        tools:replace="android:theme">
        <activity android:name=".Activities.ConsultaReunionesActivity"></activity>
        <activity android:name=".Activities.BorrarUsuarioActivity" />
        <activity android:name=".Activities.CrearUsuariosActivity" />
        <activity android:name=".Activities.CrearGruposActivity" />
        <activity android:name=".Activities.CrearAsignaturaActivity" />
        <activity android:name=".Activities.PerfilActivity" />
        <activity android:name=".Activities.ReunionesActivity" />
        <activity
            android:name=".Activities.UserActivity"
            android:configChanges="orientation|screenSize|keyboardHidden" />
        <activity android:name=".Activities.SignUpActivity" />
        <activity android:name=".Activities.RecuperarActivity" />
        <activity android:name=".Activities.ActivityMain">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.theartofdev.edmodo.cropper.CropImageActivity"
            android:theme="@style/Base.Theme.AppCompat" />
    </application>

</manifest>